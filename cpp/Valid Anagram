class Solution {
public:
    bool isAnagram(string s, string t) {
        bool i=0;
        sort(s.begin(), s.end());
        sort(t.begin(), t.end());
        if (s==t)
            i=1;
        return i;
        
    }
};
